const initialState = [
  { name: 'Todo',  id: 0 },
  { name: 'Doing', id: 1 },
  { name: 'Done',  id: 2 }
]

const boards = (state = initialState, action) => {
  return state;
}

export default boards;
